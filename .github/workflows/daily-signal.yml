name: Update Daily 512890 Signal

on:
  push:
    branches:
      - main
    paths:
      - ".github/workflows/daily-signal.yml"
      - "scripts/update_daily_snapshot.py"
      - "strategy/market_fetcher.py"
      - "requirements.txt"
  schedule:
    # 18:15 Asia/Shanghai, after A-share market data should be settled.
    - cron: "15 10 * * 1-5"
  workflow_dispatch:

concurrency:
  group: daily-512890-signal
  cancel-in-progress: true

permissions:
  contents: write

jobs:
  update:
    runs-on: ubuntu-latest
    timeout-minutes: 45
    steps:
      - name: Checkout latest main
        uses: actions/checkout@v4
        with:
          ref: main
          fetch-depth: 0

      - name: Sync latest remote main
        run: |
          git fetch origin main
          git checkout main
          git pull --ff-only origin main

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: "pip"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Update daily signal snapshot from real market data
        run: python scripts/update_daily_snapshot.py --lookback-days 820

      - name: Run tests
        run: pytest -q

      - name: Commit updated data
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add data/live_prices.csv data/latest_signal.json data/auto_portfolio.json data/signal_history.csv data/update_log.json
          if git diff --cached --quiet; then
            echo "No data changes to commit."
          else
            git commit -m "chore: update real 512890 market history"
            git pull --rebase origin main
            git push origin main
          fi
